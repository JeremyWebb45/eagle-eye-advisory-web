# Environment tier: 'development' or 'production'
TIER=development

# PostgreSQL Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=password
POSTGRES_DB=eagle_eye_db
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# JWT Configuration
# Generate a secure secret key for production (e.g., using: python -c "import secrets; print(secrets.token_urlsafe(32))")
JWT_SECRET_KEY=your-secret-key-change-in-production
